api:
  dashboard: true
  debug: true

entryPoints: # точки ввхода
  http:
    address: ":80"
  ping:
    address: ":8082"

ping:
  entryPoint: "ping"

providers:
  docker:
    endpoint: "unix:///var/run/docker.sock"
    exposedByDefault: false


#log:
# level: "INFO"
#  filePath: "var/log/traefik/traefik.log"
#accessLog:
# filePath: "var/log/traefik/access.log"